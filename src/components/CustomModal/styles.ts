import { Modal } from 'antd';
import styled from 'styled-components';

export const StyledModal = styled(Modal)`
  border-radius: 8px;
  overflow: hidden;
  
  .ant-modal-body {
    padding: 0;
  }
`;
